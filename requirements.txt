streamlit>=1.47.0
Authlib>=1.3.2   # st.login (Google OIDC sign-in)
httpx>=0.27      # required by Authlib's starlette client (st.login 500s without it)
requests>=2.31   # Supabase PostgREST credit ledger
pandas>=2.0.0
numpy>=1.26.0
plotly>=5.22.0
pyarrow>=14.0   # parquet data files (data/ folder)
fpdf2>=2.8.0    # PDF reports
matplotlib>=3.8 # charts embedded in PDF reports
# Optional, LOCAL ONLY (not on PyPI for cloud installs) — Kotak Neo live feed:
#   pip install neo-api-client
# The app degrades gracefully without it (lazy import in the Live mode).
